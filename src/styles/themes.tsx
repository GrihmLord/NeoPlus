// Path: src/styles/themes.js

import colors from './colors';

const lightTheme = {
  background: colors.background,
  text: colors.text,
  accent: colors.accent,
};

const darkTheme = {
  background: '#1c1c1c',
  text: '#f5f5f5',
  accent: colors.accent,
};

export { lightTheme, darkTheme };
